import{lazy,Suspense,useEffect,useState}from"react";import{normalizeRest}from"./lib/normalize";import{useEntityStore}from"./store/useEntityStore";import"./styles.css";
const modules={catalog:lazy(()=>import("./features/catalog/Catalog")),analytics:lazy(()=>import("./features/analytics/Analytics")),workflows:lazy(()=>import("./features/workflows/Workflows")),customers:lazy(()=>import("./features/customers/Customers")),billing:lazy(()=>import("./features/billing/Billing")),settings:lazy(()=>import("./features/settings/Settings"))};type ModuleKey=keyof typeof modules;const labels:Record<ModuleKey,string>={catalog:"Catalog",analytics:"Analytics",workflows:"Workflows",customers:"Customers",billing:"Billing",settings:"Settings"};
export default function App(){const[active,setActive]=useState<ModuleKey>("catalog");const ingest=useEntityStore(state=>state.ingest);useEffect(()=>{fetch("/api/rest/entities").then(r=>r.json()).then(normalizeRest).then(ingest).catch(()=>ingest(normalizeRest({data:Array.from({length:12},(_,i)=>({id:String(i+1),name:"Entity "+(i+1),status:i%3===0?"paused":"active",score:72+i,updatedAt:"Today"}))})))},[ingest]);const Active=modules[active];return <div className="shell"><aside><div className="brand"><b>SO</b><div>Scalable UI<small>Orchestration Layer</small></div></div><nav>{(Object.keys(modules)as ModuleKey[]).map(key=><button key={key} className={active===key?"active":""} onClick={()=>setActive(key)}>{labels[key]}</button>)}</nav><footer>Built by<br/><strong>Sravani Elavarthi</strong></footer></aside><main><Suspense fallback={<div className="loading">Loading feature module…</div>}><Active/></Suspense></main></div>}
